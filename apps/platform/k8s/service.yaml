apiVersion: v1
kind: Service
metadata:
  name: bvrinfra-platform-service
  namespace: platform

spec:
  selector:
    app: bvrinfra-platform

  ports:
  - port: 80
    targetPort: 8080

  type: ClusterIP
