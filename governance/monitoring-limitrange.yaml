apiVersion: v1
kind: LimitRange

metadata:
  name: monitoring-default-limits
  namespace: monitoring

spec:
  limits:
  - default:
      cpu: "500m"
      memory: "1Gi"

    defaultRequest:
      cpu: "100m"
      memory: "256Mi"

    type: Container
